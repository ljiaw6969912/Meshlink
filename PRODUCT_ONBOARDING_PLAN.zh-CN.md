# Meshlink 产品化入网方案

## 1. 背景

当前 Meshlink 已经具备核心组网能力，但使用方式偏工程化：

- 用户需要理解 hub / spoke。
- 用户需要手动管理 JSON 配置。
- 用户需要理解 CA、节点证书、私钥、server_name、路由、服务名。
- 用户需要自己判断服务状态、证书路径、虚拟 IP 和远程桌面目标。

这不适合普通用户，也不适合做成商业化软件。

产品化目标是把复杂配置隐藏起来，让用户只面对两个动作：

```text
创建组网
加入组网
```

高级配置仍然保留，但默认隐藏在“高级设置”中。

## 2. 产品目标

Meshlink 应该被设计成一个私有远程桌面组网工具，而不是通用 VPN 或代理工具。

核心目标：

- 用户不需要编辑 JSON。
- 用户不需要手动创建证书。
- 用户不需要理解 CA 和 CSR。
- 用户不需要手动配置节点私钥。
- 用户只需要选择“创建组网”或“加入组网”。
- 组网后软件自动显示在线设备列表。
- 点击设备即可远程桌面连接。
- 配置、证书、服务安装、启动、更新都由软件托管。

非目标：

- 不做全局代理。
- 不做公网出口转发。
- 不默认支持 `0.0.0.0/0` 全隧道。
- 不把协议设计成规避检测工具。
- 不让普通用户接触危险路由配置。

## 3. 用户入口

软件首次启动时只显示两个主入口：

```text
创建组网
加入组网
```

底部提供：

```text
高级设置
诊断工具
软件更新
```

高级设置不作为默认入口。

## 4. 模式一：创建组网

### 4.1 用户看到的流程

用户点击“创建组网”后，界面只要求填写：

```text
组网名称
本机名称
监听端口
```

默认值：

```text
组网名称：我的组网
本机名称：当前电脑名
监听端口：8443
虚拟网段：10.77.0.0/24
本机虚拟 IP：10.77.0.1
```

用户点击：

```text
启动组网服务
```

软件自动完成：

- 创建 CA。
- 创建 hub 节点证书。
- 创建 hub 配置。
- 安装或更新 `MeshlinkAgent` 服务。
- 启动服务。
- 初始化虚拟网卡。
- 创建本地状态目录。
- 启动组网状态监听。

成功后显示：

```text
组网已创建
本机地址：10.77.0.1
监听端口：8443
服务状态：运行中
```

### 4.2 公网映射提示

软件不自动操作路由器，只提示用户：

```text
请在路由器上把公网 TCP 端口映射到本机 8443。
如果你使用域名，请把域名解析到当前公网地址。
```

提供检测按钮：

```text
检测公网可达性
```

检测内容：

- 本机服务是否监听。
- 本机防火墙是否允许。
- 用户填写的域名或公网 IP 是否能解析。
- 用户填写的公网端口是否能从外部访问。

公网检测可以后续实现，第一版可以只做本机监听检测和提示。

## 5. 生成邀请

### 5.1 用户看到的内容

hub 页面提供按钮：

```text
邀请新设备
```

点击后生成：

```text
服务器地址：example.com:8443
验证码：392817
邀请链接：meshlink://join?v=1&server=example.com:8443&token=...
有效期：10 分钟
使用次数：1 次
```

用户可以复制：

```text
邀请链接
验证码
```

也可以显示二维码。

### 5.2 邀请参数

邀请由两部分组成：

```text
长随机 token
6 位数字验证码
```

设计原因：

- token 放在邀请链接里，提供足够强度。
- 6 位验证码用于人工确认，方便口头传递和防误操作。
- 两者都正确才允许加入。

邀请限制：

- 默认 10 分钟过期。
- 默认只能使用一次。
- 连续失败 5 次后作废。
- hub 记录邀请创建时间、使用时间、来源地址。

## 6. 模式二：加入组网

### 6.1 用户看到的流程

用户点击“加入组网”后，只需要填写：

```text
邀请链接
验证码
本机名称
```

默认本机名称使用 Windows 电脑名。

用户点击：

```text
加入并启动
```

软件自动完成：

- 本机生成节点私钥。
- 本机生成 CSR。
- 连接 hub 的入网接口。
- 提交 token、验证码、本机名称和 CSR。
- 接收 hub 签发的节点证书。
- 接收 CA 证书和推荐配置。
- 保存 spoke 配置。
- 安装或更新 `MeshlinkAgent` 服务。
- 启动服务。
- 显示组网设备列表。

### 6.2 关键安全原则

spoke 私钥必须在 spoke 本机生成。

hub 不应该生成 spoke 私钥，也不应该把私钥发给 spoke。

正确流程：

```text
spoke 本机生成私钥
spoke 本机生成 CSR
hub 只签发 CSR
hub 返回节点证书
```

这样即使 hub 被误操作，spoke 私钥也不会离开自己的电脑。

## 7. 入网接口设计

hub 内部增加一个轻量 enroll API，监听在 hub 服务端口或单独管理端口上。

第一版建议复用 hub 的 TCP/TLS 服务，增加 enrollment HTTP 服务会更容易实现和调试。

建议接口：

```text
GET  /enroll/health
POST /enroll/request
```

### 7.1 POST /enroll/request

请求：

```json
{
  "token": "长随机 token",
  "code": "392817",
  "node_name": "laptop",
  "csr_pem": "-----BEGIN CERTIFICATE REQUEST-----..."
}
```

响应：

```json
{
  "ca_pem": "-----BEGIN CERTIFICATE-----...",
  "cert_pem": "-----BEGIN CERTIFICATE-----...",
  "config": {
    "node_id": "laptop",
    "mode": "spoke",
    "connect": "example.com:8443",
    "server_name": "example.com",
    "virtual_ip": "10.77.0.2",
    "routes": [],
    "mtu": 1280,
    "device": {
      "type": "tun",
      "name": "meshlink0"
    },
    "setup": {
      "enabled": true,
      "address": "10.77.0.2/24",
      "routes": []
    }
  }
}
```

注意：

- 响应不包含 spoke 私钥。
- spoke 私钥只保存在 spoke 本机。
- hub 只保存签发记录和设备信息。

## 8. 本地文件布局

产品化后，普通用户不直接关心文件，但内部建议固定结构。

```text
Meshlink/
  bin/
    mesh-agent.exe
    mesh-desktop.exe
    meshctl.exe
    mesh-update-server.exe
  configs/
    active.json
    desktop-state.json
  certs/
    ca.pem
    node.pem
    node-key.pem
  invites/
    invites.json
  logs/
    MeshlinkAgent.log
    MeshlinkAgent.status.json
  updates/
```

hub 模式：

```text
certs/ca.pem
certs/ca-key.pem
certs/hub.pem
certs/hub-key.pem
configs/active.json
invites/invites.json
```

spoke 模式：

```text
certs/ca.pem
certs/node.pem
certs/node-key.pem
configs/active.json
```

## 9. UI 结构

### 9.1 首页

```text
Meshlink
私有远程桌面组网

[创建组网]
[加入组网]

高级设置
诊断
软件更新
```

### 9.2 创建组网页

```text
创建组网

组网名称：[我的组网]
本机名称：[home-pc]
监听端口：[8443]

[启动组网服务]

状态：运行中
本机虚拟 IP：10.77.0.1

[邀请新设备]
```

### 9.3 邀请设备弹窗

```text
邀请新设备

服务器地址：[example.com:8443]
验证码：392817
有效期：10 分钟

邀请链接：
meshlink://join?v=1&server=example.com:8443&token=...

[复制链接]
[复制验证码]
[关闭]
```

### 9.4 加入组网页

```text
加入组网

邀请链接：[meshlink://join?...]
验证码：[392817]
本机名称：[laptop]

[加入并启动]
```

### 9.5 设备列表页

```text
设备列表

● home-pc     10.77.0.1    本机在线
● laptop      10.77.0.2    在线
○ office-pc   10.77.0.3    离线

[远程桌面]
[诊断]
[复制 IP]
```

### 9.6 高级设置

高级设置里保留现有能力：

- JSON 配置编辑。
- 证书管理。
- 服务安装、启动、停止、卸载。
- 日志读取。
- 诊断详情。
- 更新服务配置。

但默认折叠，不作为主流程展示。

## 10. 设备管理

hub 需要维护设备注册表：

```json
{
  "nodes": [
    {
      "node_id": "laptop",
      "virtual_ip": "10.77.0.2",
      "cert_fingerprint": "SHA256:...",
      "created_at": "2026-07-01T10:00:00Z",
      "last_seen": "2026-07-01T10:10:00Z",
      "status": "online"
    }
  ]
}
```

支持操作：

- 重命名设备。
- 禁用设备。
- 删除设备。
- 吊销证书。
- 重新生成邀请。

第一版可以只做：

- 查看设备。
- 删除设备。
- 重新邀请。

证书吊销可以后续做 CRL 或本地拒绝列表。

## 11. 虚拟 IP 分配

第一版可以采用简单分配：

```text
hub 固定：10.77.0.1
spoke 从：10.77.0.2 开始递增
```

分配规则：

- 记录已分配 IP。
- 删除设备后 IP 可选择保留，不立即复用。
- 避免两个设备拿到同一个虚拟 IP。

后续可以支持：

- 手动指定 IP。
- 多网段。
- 多 hub。

## 12. 安全设计

### 12.1 邀请安全

- token 至少 128 bit 随机。
- 6 位验证码只用于辅助确认。
- token + code 同时正确才允许加入。
- 邀请默认 10 分钟过期。
- 邀请默认使用一次后作废。
- 失败次数限制。
- hub 记录入网日志。

### 12.2 证书安全

- CA 私钥只保存在 hub 本机。
- spoke 私钥只保存在 spoke 本机。
- hub 不下发 spoke 私钥。
- 节点证书包含节点名和虚拟 IP。
- 节点证书指纹显示在设备详情中。

### 12.3 路由安全

默认禁止：

```text
0.0.0.0/0
::/0
```

默认只允许：

```text
10.77.0.0/24
用户明确配置的内网网段
```

产品定位必须保持：

```text
远程桌面组网
私有设备访问
不提供通用代理
```

## 13. 错误处理

常见错误需要用人话提示。

### 13.1 hub 启动失败

```text
组网服务启动失败

可能原因：
1. 没有管理员权限
2. 端口已被占用
3. 虚拟网卡创建失败
4. 防火墙阻止监听
```

### 13.2 spoke 加入失败

```text
加入失败

可能原因：
1. 服务器地址无法连接
2. 验证码错误
3. 邀请已过期
4. 邀请已被使用
5. 服务器证书不匹配
```

### 13.3 远程桌面失败

```text
远程桌面不可达

隧道状态：正常
目标 IP：10.77.0.2
RDP 端口：不可达

请确认目标机器已开启远程桌面。
```

## 14. 与现有系统的关系

现有 JSON 配置、证书、服务、状态文件都可以保留。

产品化只是新增一层向导：

```text
向导 UI -> 自动生成 JSON -> 调用现有 agent -> 状态回读
```

不要推翻现有架构。

第一阶段应该复用：

- `mesh-agent.exe`
- `meshctl` 里的证书能力
- `runner.StatusPath`
- 当前状态文件
- 当前桌面端设备列表
- 当前更新服务

新增：

- invite 管理。
- enroll API。
- hub/spoke 向导。
- CSR 签发流程。
- 简化首页。

## 15. 传输协议插件化

Meshlink 后续应该把“底层传输协议”做成可扩展模块，让产品能在不同网络环境下快速迭代。

这里的目标不是让用户随意修改报文，也不是把安全协议做成不可审计的黑盒，而是把传输层抽象成稳定接口：

```text
组网身份 / 证书 / 路由 / 设备列表
        |
传输协议接口
        |
TCP/TLS、HTTPS Stream、后续协议
```

这样一个协议在某些网络环境里连接质量不好时，可以升级或切换到另一个内置协议，而不需要重写组网、证书、设备列表和远程桌面逻辑。

### 15.1 第一批内置协议

第一版建议内置两个协议：

| 协议 ID | 界面名称 | 定位 | 状态 |
| --- | --- | --- | --- |
| `tcp_tls_v1` | 标准模式 | 当前 TCP + TLS + mTLS + Meshlink 帧格式，延迟低、实现简单、适合自托管直连 | 第一版默认 |
| `https_stream_v1` | 兼容模式 | 基于 HTTPS 长连接、WebSocket 或 HTTP/2 Stream 承载 Meshlink 帧，便于穿过常见企业网络和反向代理 | 第二阶段实现 |

用户界面不应该展示协议细节，只展示：

```text
连接模式：标准模式
连接模式：兼容模式
```

高级设置里可以显示协议 ID 和版本号，方便排查问题。

### 15.2 协议选择规则

创建组网时，hub 页面增加：

```text
连接模式：[标准模式 v1]
```

默认使用 `tcp_tls_v1`。

高级用户可以在下拉框里选择：

```text
标准模式 v1
兼容模式 v1
```

加入组网时，spoke 不应该让普通用户手动猜协议。

邀请链接必须携带协议信息：

```text
meshlink://join?v=1&server=example.com:8443&protocol=tcp_tls_v1&token=...
```

spoke 读取邀请链接后自动选择协议。

如果 spoke 客户端不支持该协议，提示：

```text
当前客户端版本不支持服务器使用的连接模式。
请先升级 Meshlink 后再加入组网。
```

### 15.3 配置结构

后续配置建议新增 `transport` 字段，把连接地址和协议选择集中起来。

hub 示例：

```json
{
  "node_id": "home-hub",
  "mode": "hub",
  "transport": {
    "protocol": "tcp_tls_v1",
    "listen": "0.0.0.0:8443"
  }
}
```

spoke 示例：

```json
{
  "node_id": "laptop",
  "mode": "spoke",
  "transport": {
    "protocol": "tcp_tls_v1",
    "connect": "example.com:8443",
    "server_name": "example.com"
  }
}
```

为了兼容现有版本，第一阶段可以继续读取旧字段：

```text
listen
connect
server_name
```

但新 UI 生成配置时统一写入 `transport`。

### 15.4 传输接口边界

不同协议只负责“把字节流安全地送到对端”，不负责决定设备身份和路由。

传输接口应该抽象成：

```text
Dial(ctx, config) -> secure stream
Listen(ctx, config) -> secure listener
ProtocolID() -> tcp_tls_v1
```

上层复用现有逻辑：

- 节点证书校验。
- 设备身份识别。
- Meshlink 帧读写。
- 虚拟网卡收发。
- 设备列表和心跳。
- 断线重连。

这样新增协议时，只替换连接建立方式，不碰组网核心。

### 15.5 协议协商

hub 状态里需要声明支持的协议：

```json
{
  "supported_protocols": [
    "tcp_tls_v1",
    "https_stream_v1"
  ],
  "active_protocol": "tcp_tls_v1"
}
```

spoke 连接前按邀请链接选择协议。

连接成功后，双方在握手阶段确认：

```text
协议 ID
协议版本
节点 ID
证书指纹
虚拟 IP
```

如果协议不匹配，连接应该直接失败，并写入清晰日志：

```text
协议不匹配：服务器使用 tcp_tls_v1，客户端请求 https_stream_v1。
```

### 15.6 安全边界

协议可扩展，但安全规则不能随协议下放给用户随意修改。

固定规则：

- 私钥仍然只在本机生成。
- CA 私钥只保存在 hub 本机。
- 节点身份仍然由证书和指纹确认。
- 禁止默认全局代理和公网出口转发。
- 禁止普通用户编辑握手字段、证书校验规则和加密参数。
- 不提供“随机伪装成其他业务协议”的配置项。

这样可以兼顾：

- 网络兼容性。
- 后续快速迭代。
- 安全可审计。
- 产品合规边界。

### 15.7 UI 展示

创建组网页：

```text
连接模式：[标准模式 v1 ▼]
```

加入组网页：

```text
邀请链接：[meshlink://join?...]
连接模式：标准模式 v1
```

加入页的连接模式默认只读，由邀请链接自动带出。

高级设置允许手动选择，但要提示：

```text
连接模式必须和服务器一致，否则无法加入组网。
```

设备详情页显示：

```text
连接模式：标准模式 v1
来源地址：36.248.123.3
证书指纹：SHA256:...
```

### 15.8 实现阶段

阶段 A：抽象当前协议。

- 把现有 TCP/TLS 连接封装为 `tcp_tls_v1`。
- 不改变当前运行行为。
- 配置中开始写入 `transport.protocol`。
- UI 增加连接模式下拉框，但第一版只有标准模式。

阶段 B：实现兼容模式。

- 增加 `https_stream_v1`。
- 支持 HTTPS 长连接、WebSocket 或 HTTP/2 Stream 中的一种。
- 邀请链接携带协议 ID。
- spoke 自动按邀请链接选择协议。

阶段 C：协议版本管理。

- 每个协议有独立版本号。
- manifest 和更新机制记录客户端支持的协议列表。
- hub 可以提示低版本客户端升级。

阶段 D：后续扩展。

- 新协议以插件注册表方式加入。
- UI 只展示稳定内置协议。
- 实验协议只能在开发版或高级开关里出现。

## 16. 迭代计划

### 阶段一：简化首页

目标：

- 首页只保留“创建组网 / 加入组网”。
- 现有功能移入高级设置。

交付：

- 新首页。
- 创建组网 UI 草版。
- 加入组网 UI 草版。

### 阶段二：本机自动创建 hub

目标：

- 自动创建 CA。
- 自动签发 hub 证书。
- 自动生成 hub 配置。
- 自动安装并启动服务。

交付：

- 一键创建 hub。
- 显示本机虚拟 IP。
- 显示服务状态。

### 阶段三：邀请机制

目标：

- hub 生成邀请 token 和 6 位验证码。
- 邀请有效期和使用次数限制。
- 邀请列表和日志。

交付：

- 邀请弹窗。
- 邀请文件。
- 邀请校验逻辑。

### 阶段四：spoke 自动加入

目标：

- spoke 本地生成私钥和 CSR。
- spoke 提交邀请信息。
- hub 签发证书。
- spoke 自动保存配置并启动服务。

交付：

- 加入组网向导。
- enroll API。
- 自动生成 spoke 配置。

### 阶段五：设备管理

目标：

- hub 维护设备注册表。
- 支持删除设备。
- 支持禁用设备。
- 支持查看证书指纹。

交付：

- 设备管理页。
- 简单拒绝列表。

### 阶段六：产品打磨

目标：

- 错误提示口语化。
- 自动诊断。
- 更新服务集成。
- 首次启动引导。

交付：

- 面向普通用户的完整流程。
- 高级设置独立折叠。
- 安装包或便携包。

## 17. 第一版最小可行产品

第一版只做这些：

```text
1. 创建组网
2. 生成邀请
3. 加入组网
4. 自动启动服务
5. 显示设备列表
6. 点击设备远程桌面
```

暂不做：

```text
1. 复杂 ACL
2. 多 hub
3. 多网段管理
4. 证书吊销列表
5. 公网自动检测
6. 路由器自动映射
```

这样能最快把产品形态从“工程工具”变成“普通用户能用的软件”。

## 18. 关键结论

产品方向应该从：

```text
配置工具
```

转成：

```text
入网向导 + 设备列表 + 远程桌面入口
```

普通用户只应该看到：

```text
创建组网
加入组网
设备列表
远程桌面
```

证书、JSON、服务、路由、更新、日志，都应该在后台自动处理，或者放进高级设置。
