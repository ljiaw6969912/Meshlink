# 简化组网发布检查

用户当前要求：同一程序可作为服务器或客户端；客户端用链接、验证码和一个“连接”按钮接入；服务器自己也作为可访问节点；删除高级设置；启动自动申请管理员权限。

最新要求替代此前 A 只运行协调、不作为节点的约定。服务器仍不转发 B/C 的业务流量；服务器本机拥有独立节点证书与虚拟网卡，QUIC 与协调探测共用映射的 UDP 端口。

## 实施与验收

- [x] 统一连接入口，首次接入和身份复用走同一流程；拒绝失败配置直接启动服务。
- [x] 两份中文/空格空目录、同名客户端：真实 Windows 服务、Wintun、TLS 控制握手和重连身份保持测试通过。
- [x] 删除桌面及 Web 高级设置；管理员权限在打开桌面前申请。
- [x] 默认长期、多设备邀请；更新接入码不重启连接服务。
- [x] 服务器和客户端身份分目录保存，切换角色保留原配置。
- [x] 服务器本机独立身份、同端口探测与 QUIC 双向实际数据测试。
- [x] 真实 Windows 服务器兼节点测试、端口占用失败测试。
- [x] 自动配置程序和虚拟网卡所需的 Windows 防火墙规则。
- [x] 全套测试、独立复核、最新二进制编译。
- [x] 停止相关服务与进程，覆盖固定目录，原配置保留并升级 A 本机节点。
- [x] 生成只有一个 meshlink 顶层文件夹、不带配置的 ZIP；校验文件清单和散列。

发布位置：`C:\Users\Administrator\Desktop\wireguard\release\meshlink` 与同目录 `meshlink-无配置.zip`。本次源码来自 b446 工作树，现已按完整快照同步到主目录 `main`。

本地真实测试不能替代 B/C 公网现场验收；远程桌面的 Windows 系统服务与账号状态单独由诊断说明，不把“协调连接已建立”等同于“远程桌面已可用”。

## 本次验证记录

- 版本 0.1.6，构建时间 2026-09-20T09:46:28Z；`scripts/build.ps1` 完成无缓存全套 Go 测试及 Windows/Linux 编译。
- 管理员下的 Windows 集成测试通过：真实服务器本机 Wintun 节点、服务器重启保持身份和邀请、两个中文/空格空目录的同名客户端首次连接及原身份重连。
- A 运行服务已从旧协调器升级为服务器兼节点：`LJW-node`、`10.77.0.1`，实际状态 connected，TCP/UDP 同时监听 `0.0.0.0:3222`。
- 覆盖前停止所有相关进程，备份并逐文件校验原有 21 个运行数据文件；旧 CA、B/C 的证书指纹和虚拟 IP 不变。旧限额邀请码升级为长期多设备邀请码。
- ZIP 共 13 个文件，逐项校验允许清单和散列；打包时再次停服，25 个运行数据文件保持不变。已修复 Windows PowerShell 5.1 替换旧 ZIP 时空备份路径转换问题。
- 自动提权实际验证：普通权限启动进程 13436 后退出，实际桌面进程 18400 的 TokenElevation 为 true。
- 视觉验收未完成：Computer Use 激活窗口失败，`GetCursorPos failed: 拒绝访问 (0x80070005)`；没有将代码检查描述成截图验收。
- 清理后，部署、打包及提权验证记录归档到 `C:\Users\Administrator\.codex\backups\wireguard\root-cleanup-20260920-212452\diagnostics\abc-diagnostics`；旧身份备份一并保留，过期二进制和临时测试数据已清除。
