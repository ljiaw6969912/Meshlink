# Meshlink 简化双模式产品方案

## 1. 核心判断

当前产品不应该继续朝“配置工具”发展。

普通用户不应该看到：

- JSON 配置。
- CA 名称。
- 证书路径。
- 签发节点证书。
- 服务名。
- 配置文件路径。
- 虚拟网卡细节。
- 路由参数。

第一版产品应该只围绕三个概念：

```text
服务器模式
客户端模式
组网机群节点列表
```

用户真正需要完成的动作只有两个：

```text
我创建一个可接入的服务器
我用接入链接和接入码加入服务器
```

## 2. 产品定位

Meshlink 是一个自托管远程桌面组网软件。

它的默认用途是：

- 家里电脑作为服务器。
- 外面电脑作为客户端。
- 双方组成一个私有虚拟网络。
- 用户通过节点列表点击远程桌面。

它不是：

- 通用代理软件。
- 公网出口转发工具。
- 面向普通用户的复杂 VPN 配置器。
- 需要用户理解证书和配置文件的工程工具。

## 3. 主界面结构

软件主界面只保留三块：

```text
顶部：模式切换
[服务器模式] [客户端模式]

中间：当前模式操作区
服务器模式：启动/停止、接入地址、接入码
客户端模式：接入链接、接入码、加入/退出

下方：组网机群节点列表
```

高级设置可以存在，但默认收起。

## 4. 服务器模式

### 4.1 用户看到的界面

服务器模式只显示必要信息：

```text
服务器模式

域名或公网地址
[ example.com ]

监听端口
[ 8443 ]

[启动服务器] [停止服务器]

接入链接
[ meshlink://join?server=example.com:8443&token=... ]

接入码
[ 392817 ]  09:58 后过期

[复制链接] [复制接入码] [重新生成]
[ ] 长期接入码
```

域名或公网地址可以为空。

如果为空，软件可以先用本机局域网地址或提示用户：

```text
请填写映射到本机的公网域名或公网 IP。
```

### 4.2 启动服务器后自动完成

用户点击“启动服务器”后，软件自动完成：

- 创建 CA。
- 创建服务器证书。
- 生成服务器配置。
- 创建或启用虚拟网卡。
- 设置服务器虚拟 IP 为 `10.77.0.1`。
- 启动监听端口。
- 启动入网接口。
- 生成短期一次性接入链接。
- 生成 6 位接入码。
- 启动组网机群节点列表状态刷新。

用户不需要知道 CA、证书和 JSON 文件在哪里。

### 4.3 接入码规则

默认接入码规则：

- 6 位数字。
- 默认 10 分钟有效。
- 成功接入一次后立即失效。
- 连续输错 5 次后作废。
- 服务器界面显示倒计时。
- 服务器界面保留“重新生成”按钮。

服务器启动成功后自动生成一个短期一次性接入码。

用户也可以点击“重新生成”主动换码。

### 4.4 长期接入码

长期接入码不是默认行为，需要用户主动勾选：

```text
[ ] 长期接入码
```

开启后：

- 接入码长期有效。
- 界面明显显示“长期有效”。
- 用户可以随时重置。
- 服务端只保存哈希，不保存明文。
- 入网接口需要限速。
- 可以限制最大接入设备数。

长期接入码适合用户自己的多台设备长期加入，不适合作为公开邀请方式。

## 5. 客户端模式

### 5.1 用户看到的界面

客户端模式主入口：

```text
客户端模式

接入链接
[ meshlink://join?server=example.com:8443&token=... ]

接入码
[ 392817 ]

[加入网络] [退出网络]

手动填写服务器地址
```

“手动填写服务器地址”默认折叠。

展开后显示：

```text
服务器地址
[ example.com:8443 ]
```

### 5.2 加入网络后自动完成

用户点击“加入网络”后，软件自动完成：

- 解析接入链接。
- 提取服务器地址、token 和协议模式。
- 本机生成私钥。
- 本机生成 CSR。
- 连接服务器入网接口。
- 提交 token、接入码、本机名称和 CSR。
- 下载 CA 证书。
- 下载服务器签发的客户端节点证书。
- 生成客户端配置。
- 创建或启用虚拟网卡。
- 启动客户端服务。
- 自动刷新组网机群节点列表。

客户端私钥必须在客户端本机生成。

服务器只签发 CSR，不生成客户端私钥，也不下发客户端私钥。

## 6. 接入链接

接入链接用于降低手动输入成本。

建议格式：

```text
meshlink://join?server=example.com:8443&token=...&protocol=tcp_tls_v1
```

字段说明：

- `server`：服务器公网域名或地址。
- `token`：长随机 token。
- `protocol`：连接模式，第一版默认 `tcp_tls_v1`。

接入链接不单独授权。

客户端仍然需要输入 6 位接入码。

这样可以兼顾：

- 链接复制方便。
- token 强度足够。
- 6 位码方便人工确认。
- 链接泄露后仍然需要接入码。

## 7. 组网机群节点列表

节点列表是主界面核心，不是诊断附属页。

服务器模式和客户端模式都显示同一套列表：

```text
● home-server   10.77.0.1   本机在线
● laptop        10.77.0.2   在线
○ office-pc     10.77.0.3   离线
```

节点列表需要支持动态上下线：

- 节点上线时变成绿色。
- 节点离线时变成灰色。
- 本机节点明确标记“本机”。
- 远程节点显示来源 IP。
- 列表定时刷新。
- 手动刷新按钮保留。

点击节点后，右侧或下方面板显示：

```text
节点名称
虚拟 IP
在线状态
来源 IP
连接时间
最近在线时间
证书指纹
连接模式

[远程桌面] [复制 IP] [诊断]
```

远程桌面入口默认使用节点的虚拟 IP。

## 8. 高级设置

高级设置默认隐藏，只给排查问题和高级用户使用。

高级设置可以包含：

- JSON 配置编辑。
- 证书文件位置。
- 服务安装、启动、停止、卸载。
- 日志读取。
- 诊断详情。
- 更新服务配置。
- 协议模式选择。
- 虚拟网段设置。

普通主流程不依赖高级设置。

## 9. 后台文件布局

普通用户不需要看到文件，但内部可以统一为：

```text
Meshlink/
  bin/
  configs/
    active.json
    desktop-state.json
    devices.json
  certs/
    ca.pem
    ca-key.pem
    node.pem
    node-key.pem
  invites/
    invites.json
  logs/
  updates/
```

服务器模式保留：

```text
certs/ca.pem
certs/ca-key.pem
certs/node.pem
certs/node-key.pem
configs/active.json
configs/devices.json
invites/invites.json
```

客户端模式保留：

```text
certs/ca.pem
certs/node.pem
certs/node-key.pem
configs/active.json
```

## 10. 安全规则

第一版必须坚持这些规则：

- 服务器自动创建 CA。
- CA 私钥只保存在服务器本机。
- 客户端私钥只在客户端本机生成。
- 客户端通过 CSR 申请证书。
- 服务器只签发证书，不下发客户端私钥。
- 接入码默认短期一次性。
- 长期接入码需要用户主动开启。
- 默认禁止全局代理路由。
- 默认只做私有设备访问和远程桌面入口。

## 11. 错误提示

错误提示必须面向普通用户。

服务器启动失败：

```text
服务器启动失败

可能原因：
1. 没有管理员权限
2. 端口已被占用
3. 防火墙阻止监听
4. 虚拟网卡创建失败
```

客户端加入失败：

```text
加入网络失败

可能原因：
1. 接入地址无法连接
2. 接入码错误
3. 接入码已过期
4. 接入码已被使用
5. 当前客户端版本不支持服务器连接模式
```

远程桌面失败：

```text
远程桌面不可达

隧道状态：正常
目标设备：laptop
目标 IP：10.77.0.2
RDP 端口：不可达

请确认目标设备已经开启远程桌面。
```

## 12. 第一版 MVP 范围

第一版只做：

```text
1. 服务器模式
2. 客户端模式
3. 服务器启动后自动生成短期接入链接和接入码
4. 客户端通过接入链接和接入码加入
5. CSR 入网签发
6. 自动生成配置
7. 自动启动服务
8. 组网机群节点列表
9. 点击节点远程桌面
10. 高级设置隐藏
```

第一版不做：

```text
1. 多服务器 hub
2. 复杂 ACL
3. CRL 证书吊销列表
4. 路由器自动映射
5. 公网自动域名解析
6. 花哨的协议切换 UI
7. 面向普通用户的 JSON 编辑
```

## 13. 和上一版方案的区别

上一版更像：

```text
创建组网 / 加入组网 / 高级配置管理
```

新方案改成：

```text
服务器模式 / 客户端模式 / 节点列表
```

区别不是小改界面，而是产品心智变化：

- 用户不再理解 hub / spoke。
- 用户只理解服务器 / 客户端。
- 服务器负责发接入码。
- 客户端负责输入接入链接和接入码。
- 节点列表始终保留，作为核心使用界面。
- 高级配置从主流程中消失。

## 14. 关键结论

Meshlink 第一版应该让用户感觉像在使用一个简单的远程桌面组网软件：

```text
服务器：启动，复制接入信息
客户端：粘贴接入信息，加入
双方：在节点列表里看到彼此，点击远程桌面
```

只要普通用户还需要理解证书、JSON、服务名、配置路径，就说明产品还没有收敛到位。
