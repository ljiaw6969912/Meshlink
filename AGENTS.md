# 发布约定

- 用户指定所有后续修改后的程序统一发布到 `C:\Users\Administrator\Desktop\wireguard\release`。
- `release` 下保留固定的 `meshlink` 完整目录，以及固定的 `meshlink-无配置.zip` 外发压缩包，不另建版本号或时间戳目录。
- 根据用户 2026-09-20 的最新要求，每次编译发布后同时生成一份不带配置的压缩包，供其他机器使用；新压缩包直接覆盖同名旧包。
- 外发包内只有一个 `meshlink` 顶层文件夹，按白名单包含程序、Wintun、启动入口、使用说明、许可证和构建信息；不得包含 configs、certs、invites、logs、证书、私钥、邀请码、设备登记、桌面状态或其他运行数据。
- 本地 `release\meshlink` 继续保留现有运行配置与数据，不能为了制作无配置包而删除这些内容。
- 每次整理或覆盖发布产物前，必须先停止所有相关 Meshlink 服务和进程，并确认已经全部退出，再覆盖 `release\meshlink` 中的旧版本；不另建带版本号或时间戳的发布目录。
- 覆盖程序时保留用户配置、证书及运行数据，除非用户明确要求替换这些内容。

## 当前发布源码

- 2026-09-20 已将实际发布源码以完整快照提交到 `main`；主目录现在包含 P2P 实现、0.1.6 连接修复、服务器本机节点及自动管理员启动。
- 本次源码来自 `C:\Users\Administrator\.codex\worktrees\b446\wireguard`；后续可从同步后的主目录 `main` 编译，不得使用仍缺少 `tcp_tls_control_v2` 的旧提交覆盖程序。
- 编译来源记录在 `release\meshlink\build-metadata.json` 中。构建结果仍发布到固定目录，不能把源码目录的旧运行身份复制到发行目录。
- 以管理员权限运行 `scripts/package-clean.ps1` 生成外发包；该脚本会先停止相关服务和进程，验证包内白名单与文件哈希，随后恢复原先运行的服务。
- Git 仅提交源码、测试、脚本和文档；`release`、配置、证书、私钥、邀请码、日志及本机备份不纳入提交。
